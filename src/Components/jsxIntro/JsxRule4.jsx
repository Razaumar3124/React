let JsxRule4 = ()=>{
    //! we can't use attribute like "class" and "for" while writing the jsx. instead of that we have to use "className" and "htmlFor".
    return (
        <div>
            <label htmlFor="">JsxRule4</label>
            <p className="para1">we can't use attribute like "class" and "for" while writing the jsx. instead of that we have to use "className" and "htmlFor"</p>
        </div>
    )
}
export default JsxRule4